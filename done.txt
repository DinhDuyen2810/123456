create table users (
  id uuid primary key default gen_random_uuid(),
  username text unique not null,
  public_key text not null,
  encrypted_private_key text not null,
  salt text not null,
  created_at timestamp default now()
);
create table folders (
  folder_id uuid primary key default gen_random_uuid(),
  owner_id uuid references users(id),
  parent_id uuid references folders(folder_id), -- null nếu root
  name text not null,
  created_at timestamp default now()
);
create table files (
  file_id uuid primary key default gen_random_uuid(),
  owner_id uuid references users(id),
  folder_id uuid references folders(folder_id), -- null nếu root
  storage_path text not null,
  encrypted_file_key_owner text not null,
  original_filename text,
  mime_type text,
  created_at timestamp default now()
);
create table file_shares (
  id uuid primary key default gen_random_uuid(),
  file_id uuid references files(file_id),
  shared_with_user_id uuid references users(id),
  encrypted_file_key text not null,
  created_at timestamp default now()
);
create table folder_shares (
  id uuid primary key default gen_random_uuid(),
  folder_id uuid references folders(folder_id),
  shared_with_user_id uuid references users(id),
  created_at timestamp default now()
);
src/
 ├─ pages/
 │   ├─ Register.jsx
 │   ├─ Login.jsx
 │   ├─ Dashboard.jsx
 │   └─ FileRow.jsx
 ├─ components/
 │   ├─ FileTree.jsx
 │   └─ UploadModal.jsx
 ├─ crypto/
 │   ├─ initSodium.js
 │   ├─ keyDerivation.js
 │   ├─ keyPair.js
 │   ├─ fileEncryption.js
 │   └─ shareFileKey.js
 ├─ utils/
 │   ├─ supabase.js
 │   └─ session.js       # lưu privateKey trong session
 └─ App.jsx
